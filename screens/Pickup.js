import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Pickup = () => {
  return (
    <View>
      <Text>This is for Pickup</Text>
    </View>
  );
};

export default Pickup;

const styles = StyleSheet.create({});
